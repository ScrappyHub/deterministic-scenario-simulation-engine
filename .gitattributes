# DSSE: canonical bytes discipline (LF only) ? do not let git rewrite evidence* text=auto eol=lf# Evidence / vectors must never be EOL-normalized by accident*.ndjson -text*.json   -text*.txt    -text*.sig    -text# Scripts are LF*.ps1 text eol=lf*.md  text eol=lf